# v1.2.9

Release date: 2026-07-12

## Highlights

- Added explicit regression coverage for the ChatGPT masking case `hi my email is ade@gmail.com `.
- Hardened the selection popup Playwright coverage so the full extension suite stays stable across ChatGPT, Claude, and Gemini.
- Regrouped settings controls so protection controls and site actions are shown once, without repeated sections.

## Notes

- Browser extension version metadata is now aligned across the manifest, extension package metadata, and test harness at `1.2.9`.
- Full browser-extension Playwright coverage passed for this release.
- The local `node --test` jsdom suite remains blocked by the existing `html-encoding-sniffer` / ESM dependency issue on Node `20.10.0`.
